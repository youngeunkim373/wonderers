const baseStyle = {
  track: {
    _checked: {
      bg: "pink.main",
    },
  },
};

const label = {
  _disabled: { opacity: 0.7 },
};

const defaultProps = {
  size: "md",
};

const Switch = {
  baseStyle,
  label,
  defaultProps,
};

export default Switch;
