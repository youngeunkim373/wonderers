import type { SystemStyleObject } from '@chakra-ui/styled-system';

const baseStyle: SystemStyleObject = {
  _hover: {
    color: 'MainColor',
    textDecoration: 'none',
  },
};

const Link = {
  baseStyle,
};

export default Link;
