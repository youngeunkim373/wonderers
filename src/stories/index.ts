import { button } from "./Button";
import { label } from "./Label";
import { input } from "./Input";
import { pageTitle } from "./PageTitle";
import { heading } from "./Heading";
import { formControl } from "./FormControl";
import {
  formInput,
  formInputWithButton,
  formInputInvalid,
  formInputPassword,
} from "./FormInput";

export const Button = button;
export const Label = label;
export const Input = input;
export const PageTitle = pageTitle;
export const Heading = heading;
export const FormControl = formControl;
export const FormInput = formInput;
export const FormInputWithButton = formInputWithButton;
export const FormInputInvalid = formInputInvalid;
export const FormInputPassword = formInputPassword;
